const alturaCentimetros = 170;

const alturaMetros = 1.70;

const pesoKilogramo = 51.3;

const alturaRedondeada = Math.ceil(alturaMetros)

const pesoRedondeado = Math.floor(pesoKilogramo)

const iguales = Number.MAX_VALUE + 1 === Number.MAX_VALUE

